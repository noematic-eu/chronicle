[package]
name = "chronicle-dsl"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Chronicle author Markdown → IR"
publish = false

[dependencies]
chronicle-engine = { path = "../chronicle-engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "2"

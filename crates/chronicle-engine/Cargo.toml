[package]
name = "chronicle-engine"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Chronicle chapter engine (no I/O)"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
chronicle-dsl = { path = "../chronicle-dsl" }
